apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tests-pvc
  namespace: excellentia
spec:
  storageClassName: managed
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1500Mi