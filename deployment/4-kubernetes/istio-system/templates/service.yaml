apiVersion: v1
kind: Service
metadata:
  name: {{ include "istio-system.fullname" . }}
  labels:
    {{- include "istio-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "istio-system.selectorLabels" . | nindent 4 }}
